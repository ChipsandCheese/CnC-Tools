project('cnc-tools', 'c',
  version : '0.1',
  default_options : [
    'warning_level=3',
    'buildtype=release',
    'c_std=gnu11,c11',
])

threads_dep = dependency('threads')

subdir('Framework')
subdir('CPU Tests')